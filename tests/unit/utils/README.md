# 🧮 Testes de Utilitários e Cálculos (`unit/utils/`)

Este diretório foca em validar isoladamente todas as lógicas matemáticas, financeiras e regras de processamento bruto da aplicação, sem amarras com a interface gráfica.

---

## 📄 Códigos e Responsabilidades

- **`calculos.test.ts`**: Espelha todas as implementações em `utils/calculos.ts`. Cobre cenários precisos de indicadores zootécnicos (KPIs), divisões sensíveis por zero, verificações de status de benchmark (inclusive casos onde "menos é melhor") e contagem do pilar limitante diagnosticado pela IA.